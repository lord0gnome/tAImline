apiVersion: v1
kind: Service
metadata:
  name: {{ include "taimline.fullname" . }}
  labels:
    {{- include "taimline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "taimline.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

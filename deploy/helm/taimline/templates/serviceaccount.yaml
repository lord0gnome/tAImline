{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "taimline.serviceAccountName" . }}
  labels:
    {{- include "taimline.labels" . | nindent 4 }}
{{- end }}

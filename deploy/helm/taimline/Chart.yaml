apiVersion: v2
name: taimline
description: Zoomable life-timeline blogging app (Astro SSR + SQLite)
type: application
version: 0.15.1
# appVersion tracks the container image tag when not overridden in values.
appVersion: "0.15.1"
